{
  "$comment": "Shared severity-escalation table for the pr-review gate. Consumed by both the Codex $pr-review skill (read alongside review-criteria.md during severity normalization) and the Claude-side /pr-review skill (parsed and passed into the pr-review dynamic workflow). Canonical copy: private_dot_codex/skills/pr-review/references/severity-rules.json — the copy under ~/.claude/skills/pr-review/references/ is generated by chezmoi from this file; edit only the canonical copy. Prose gate policy (modes, acceptance, caps rationale, re-review): review-criteria.md.",
  "sentinel": "PR_REVIEW_SEVERITY_RULES_V1",
  "version": 1,
  "output_caps": {
    "important": 5,
    "suggestion": 3
  },
  "critical": {
    "$comment": "A finding is Critical when it matches ANY rule in any_of AND satisfies the guard.",
    "guard": "The finding explains a concrete merge-blocking risk grounded in the committed branch diff.",
    "any_of": [
      {
        "kind": "explicit_label",
        "specialist": "*",
        "labels": ["Critical"],
        "case_insensitive": true
      },
      {
        "kind": "confidence_threshold",
        "specialist": "code-reviewer",
        "scale": "0-100",
        "min": 90
      },
      {
        "kind": "framing_with_confidence",
        "specialist": "adversarial-reviewer",
        "framing": "needs-attention",
        "scale": "0-1",
        "min": 0.7
      },
      {
        "kind": "explicit_label",
        "specialist": "silent-failure-hunter",
        "labels": ["CRITICAL"],
        "case_insensitive": true
      },
      {
        "kind": "severity_field",
        "specialist": "security-reviewer",
        "values": ["High"],
        "case_insensitive": true
      }
    ]
  },
  "important": {
    "$comment": "Evaluated only for findings that did not qualify as Critical. A finding is Important when it matches ANY rule in any_of AND satisfies the guard.",
    "guard": "The finding is likely worth fixing before merge but is not a proven blocker.",
    "any_of": [
      {
        "kind": "explicit_label",
        "specialist": "*",
        "labels": ["Important", "High"],
        "case_insensitive": true
      },
      {
        "kind": "severity_field",
        "specialist": "security-reviewer",
        "values": ["Medium"],
        "case_insensitive": true
      },
      {
        "kind": "category_label",
        "specialist": "pr-test-analyzer",
        "labels": ["Critical Gap", "Important Improvement"]
      }
    ]
  },
  "suggestion": {
    "rule": "Remaining advisory findings are Suggestions unless the specialist explicitly marks them as positive observations (Strengths)."
  },
  "nit": {
    "rule": "Style-only, preference-only, naming-only, or speculative cleanup. Never promote nits, style preferences, speculative rewrites, or weakly grounded concerns into Critical or Important; nits do not enter the fix queue."
  },
  "incomplete_evidence": "If evidence is incomplete but the risk may be severe, keep the finding with the missing verification stated explicitly; do not silently drop it."
}
