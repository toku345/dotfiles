#!/usr/bin/env fish
if set -q FAKE_FZF_STDIN_LOG
    cat >$FAKE_FZF_STDIN_LOG
else
    cat >/dev/null
end

if set -q FAKE_FZF_SELECT
    echo $FAKE_FZF_SELECT
end

if set -q FAKE_FZF_EXIT
    exit $FAKE_FZF_EXIT
end

exit 0
